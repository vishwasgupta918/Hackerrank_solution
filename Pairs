int pairs(int k, vector<int> arr) {
    unordered_set<int> s(arr.begin(), arr.end());
    int count = 0;

    for (int num : arr) {
        if (s.find(num + k) != s.end()) {
            count++;
        }
    }
    return count;
}
